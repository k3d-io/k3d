[template]
src = "nginx.tmpl"
dest = "/etc/nginx/nginx.conf"
keys = [
    "ports",
    "settings"
]
check_cmd = "/usr/sbin/nginx -T -c {{.src}}"
reload_cmd = "/bin/sh -c 'PID=$(cat /var/run/nginx.pid 2>/dev/null || true); [ -n \"$PID\" ] && /usr/sbin/nginx -s reload || true'"
